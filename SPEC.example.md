# 작업 계약

## 요청

사용자가 체감할 수 있는 변경을 한 문단으로 적는다.

## 범위 밖

- 이번 작업에서 하지 않을 일을 적는다.

## 수용 조건

- AC-001: 사용자가 어떤 입력을 주면 어떤 결과를 확인할 수 있어야 한다.
- AC-002: 기존 동작 중 무엇이 그대로 유지되어야 한다.

## 필수 검증 명령

- CMD-001: `프로젝트의 실제 테스트 명령`
- CMD-002: `프로젝트의 실제 빌드 또는 정적 검사 명령`

각 줄은 백틱 하나로 감싼 실행 가능한 명령으로 끝난다. 백틱 뒤에 설명을 붙이지 않고, 정의하지 않은 `CMD-###` 번호를 본문 어디에도 쓰지 않는다 (runner가 SPEC 전체에서 그 패턴을 필수 명령 ID로 파싱한다). Windows에서는 PowerShell로 실행되므로 `&&` 같은 POSIX 연산자는 Git Bash 전체 경로로 감싼다: `C:\PROGRA~1\Git\bin\bash.exe -c '...'`. 무수식 `bash`는 WSL 런처로 해석되어 거부된다.

## 제약과 메모

- 수정하면 안 되는 파일, 호환성, 보안 또는 사용량 제약을 적는다.
